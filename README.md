# DatabaseJava

A simple Java project for inserting content into a database using JDBC. This repository demonstrates how to connect Java applications to a relational database and perform basic SQL operations.

---

## 🔹 Overview

This project shows how to:

- Establish a database connection using Java.
- Insert data into a database table.
- Organize Java source files and external libraries (`lib/`) for use in an Eclipse project.

It is intended for **learning purposes** and as a starter template for Java database interaction.

---

## 🛠️ Technologies Used

- Java (Standard Edition)
- JDBC (Java Database Connectivity) — a standard Java API for interacting with relational databases such as MySQL, PostgreSQL, or SQLite. :contentReference[oaicite:2]{index=2}
- Eclipse IDE project configuration (`.project` and `.classpath`)
- External libraries stored in the `lib/` directory (if any)

---

## 📁 Project Structure

